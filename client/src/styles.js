import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  container: {
    paddingTop: '0', // Add padding at the top to separate navbar from content
    paddingBottom: '0', // Add padding at the bottom for better spacing
    paddingLeft:'0',
    paddingRight:'0'
  },
}));
